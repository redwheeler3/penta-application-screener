# Seed admins — persisted as permanent admins on the access allowlist at startup.
# Copy this to initial-admins.txt (gitignored) and list each permanent admin
# email(s), one per line. Blank lines and '#' comments are ignored.
#
# A seeded admin cannot be demoted or removed in-app. Removing an email from this
# file does not remove its persisted protection.
admin@example.com
